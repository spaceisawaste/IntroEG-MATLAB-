function newClass=makeNewClass(title, number,seats)
%Creates a new class with parameters title, id Number, and an array with
%the number of seats possible for the class versus seats taken
newClass.title=title;
newClass.number=number;
newClass.seats=seats;